{
  "name": "catalog",
  "version": "1.0.0",
  "description": "Express API for managing and accessing the Wavlake catalog",
  "main": "index.js",
  "engines": {
    "node": "=v16.20.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/index.js",
    "dev": "NODE_ENV=dev concurrently \"npx tsc --watch\" \"nodemon dist/index.js --watch library --watch controllers --watch routes\"",
    "create-env": "printenv > .env",
    "gcp-build": "npx tsc",
    "build": "npx tsc"
  },
  "author": "Michael Rhee",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.1.3",
    "express": "^4.18.2",
    "knex": "^2.4.0",
    "knex-paginate": "^3.0.2",
    "loglevel": "^1.7.1",
    "pg": "^8.7.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.2.5",
    "concurrently": "^8.1.0",
    "nodemon": "^2.0.22",
    "typescript": "^5.1.3"
  }
}
