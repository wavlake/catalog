{
  "name": "catalog",
  "version": "1.0.0",
  "description": "Express API for managing and accessing the Wavlake catalog",
  "main": "index.js",
  "engines": {
    "node": "=v16.20.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "NODE_ENV=dev nodemon index.js --watch library --watch controllers --watch routes"
  },
  "author": "Michael Rhee",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.1.3",
    "express": "^4.18.2",
    "knex": "^2.4.0",
    "knex-paginate": "^3.0.2",
    "loglevel": "^1.7.1",
    "pg": "^8.7.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
